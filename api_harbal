ALTER TABLE `tbl_rates` CHANGE `rates_entrydt` `rates_entrydt` DATETIME NULL DEFAULT NULL;

ALTER TABLE `tbl_rates` CHANGE `rates_permin` `rates_permin` FLOAT NULL DEFAULT NULL;

ALTER TABLE `tbl_rates` CHANGE `rates_amount` `rates_amount` FLOAT(10,2) NULL DEFAULT NULL;

ALTER TABLE `tbl_rates` CHANGE `rates_cab` `rates_cab` INT(11) NULL DEFAULT NULL;

ALTER TABLE `tbl_rates` CHANGE `rates_to` `rates_to` FLOAT(10,2) NULL DEFAULT NULL;

ALTER TABLE `tbl_rates` CHANGE `rates_from` `rates_from` FLOAT(10,2) NULL DEFAULT NULL;

ALTER TABLE `tbl_offers` CHANGE `offer_min` `offer_min` FLOAT NULL DEFAULT NULL;
